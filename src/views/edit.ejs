<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Launch - Rocket Launch Admin</title>
    <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
    <header>
        <nav>
            <div class="container">
                <h1><a href="/">🚀 Rocket Launch Admin</a></h1>
                <ul>
                    <li><a href="/">All Launches</a></li>
                    <li><a href="/create">New Launch</a></li>
                </ul>
            </div>
        </nav>
    </header>
    
    <main>
        <div class="container">
            <h2>Edit Launch</h2>

<form method="POST" action="/edit/<%= countdown.id %>" class="launch-form">
    <div class="form-group">
        <label for="mission">Mission Name:</label>
        <input type="text" id="mission" name="mission" value="<%= countdown.mission %>" required>
    </div>
    
    <div class="form-group">
        <label for="rocket">Rocket:</label>
        <input type="text" id="rocket" name="rocket" value="<%= countdown.rocket %>" required>
    </div>
    
    <div class="form-group">
        <label for="launchDate">Launch Date & Time:</label>
        <input type="datetime-local" id="launchDate" name="launchDate" 
               value="<%= countdown.launchDate.toISOString().slice(0, 16) %>" required>
    </div>
    
    <div class="form-group">
        <label for="description">Description (optional):</label>
        <textarea id="description" name="description" rows="4"><%= countdown.description || '' %></textarea>
    </div>
    
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">Update Launch</button>
        <a href="/" class="btn btn-secondary">Cancel</a>
    </div>
</form>
        </div>
    </main>
    
    <footer>
        <div class="container">
            <p>&copy; 2025 Rocket Launch Countdown Admin. Ready for liftoff!</p>
        </div>
    </footer>
</body>
</html>